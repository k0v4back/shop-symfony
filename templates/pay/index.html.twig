{% extends 'base.html.twig' %}

{% block body %}
    {{ dump(product) }}
    <div class="container mt-3">
        <div class="row">
            <div class="col-12">
                <h3 class="mb-4">Покупка <a href="{{ path('view_one_product', {'id':product.id}) }}">{{ product.title }}</a></h3>
                <b>Кол-во: {{ quantity }}</b>
                <br>
                <b>Общая цена: {{ price }}</b>
                <br>
                <hr>
                <br>
                <form>
                    <div class="form-row">
                        <div class="form-group col-md-12">
                            <label for="inputEmail4">Ваша почта</label>
                            <input type="email" class="form-control" id="inputEmail4" placeholder="Email">
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group col-md-4">
                            <label for="inputAddress">Город</label>
                            <input type="text" class="form-control" id="inputAddress" placeholder="">
                        </div>
                    <div class="form-group col-md-4">
                        <label for="inputAddress">Адрес доставки</label>
                        <input type="text" class="form-control" id="inputAddress" placeholder="">
                    </div>
                        <div class="form-group col-md-4">
                            <label for="inputAddress">Почтовый индекс</label>
                            <input type="text" class="form-control" id="inputAddress" placeholder="">
                        </div>
                    </div>
                    <div class="form-row">
                        <h4>Тут должно быть поле оплаты, но её здесь нет:). </h4>
                        <h4> Жми на кнопку 'Купить' и следуй дальше</h4>
                    </div>

                    <button type="submit" class="btn btn-primary">Купить</button>
                </form>
            </div>
        </div>
    </div>

{% endblock %}